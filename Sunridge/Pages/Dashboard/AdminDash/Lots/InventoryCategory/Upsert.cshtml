@page
@model Sunridge.Pages.Dashboard.AdminDash.Lots.InventoryCategory.UpsertModel
<form method="post" asp-action="Upsert">

    <div class="container">

        <div class="row px-2 mx-2 backgroundWhite border">

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @if (Model.InventoryItem.Id != 0)
            {
                <input type="hidden" asp-for="InventoryItem.Id" />
            }
            <div class="col-md-12 px-3" style="border-bottom: 1px solid #325d88">
                <h2 class="text-primary">@(Model.InventoryItem.Id != 0 ? "Update" : "Create") Inventory Category</h2>
            </div>

            <div class="col-md-6 pt-3">

                @*<div class="form-group row">

                    <div class="col-md-3">
                        <label asp-for="InventoryItem.Description"></label>
                    </div>
                </div>
                <div class="form-group row">
                    <div class="col-md-9">
                        <input class="form-control" asp-for="InventoryItem.Description" />
                        <span class="text-danger" asp-validation-for="InventoryItem.Description"></span>
                    </div>

                </div>*@
                <div class="form-group">
                    <label asp-for="InventoryItem.Description"></label>
                    <input asp-for="InventoryItem.Description" class="form-control" />
                    <span asp-validation-for="InventoryItem.Description" class="text-danger"></span>
                </div>

                <div class="form-group row pl-3  pr-3">
                    <button id="update-profile-button" type="submit" class="btn btn-primary col-6">@(Model.InventoryItem.Id != 0 ? "Update" : "Create")</button>
                    <a class="btn btn-success col-6 form-control " asp-page="./Index">Back To List</a>
                </div>

            </div>

        </div>

    </div>



</form>

