@page
@model Sunridge.Pages.Home.LostFoundModel
@{
    ViewData["Title"] = "LostFound";
}

    @foreach (var item in Model.itemList)
    {
    <div class="row ">
        @*@if (item.Images.Count() > 0)
        {*@
            <div id="carouselExampleControls" class="carousel slide" data-ride="carousel" data-interval="7000">
                <div class="carousel-inner">


                    <div class="carousel-item active">
                    <img class="masthead9 bg-primary" src="/@Html.DisplayFor(modelItem =>item.Images[0].ImageURL)"/>
                        
                        
                    </div>
                    @foreach (var itemImage in item.Images)
                    {
                        @if (itemImage.Id != item.Images[0].Id)
                        {
                            <div class="carousel-item ">
                                <img class="masthead9 bg-primary" src="/@Html.DisplayFor(modelItem => itemImage.ImageURL)" />
                                
                            </div>
                        }
                    }
                </div>

                <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
        @*}*@
        <br />
    </div>

         } 




